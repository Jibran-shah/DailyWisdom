<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Quotes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="fontFamily">@font/winkyrough_variablefont_wght</item>
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <item name="card.background" type="drawable">@drawable/quote_background_night</item>
    <item name="image.tint" type="color">@color/white</item>
</resources>